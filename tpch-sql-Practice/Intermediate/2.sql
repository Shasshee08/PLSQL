SELECT
    S.S_SUPPKEY,
    S.S_NAME,
    COUNT(*) AS TOTAL_LINEITEMS
FROM
    SUPPLIER S
JOIN
    LINEITEM L ON S.S_SUPPKEY = L.L_SUPPKEY
GROUP BY
    S.S_SUPPKEY, S.S_NAME
HAVING
    COUNT(*) > 500
ORDER BY
    TOTAL_LINEITEMS DESC;
